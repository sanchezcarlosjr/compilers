class Test {
  foo:Test;
  bar(x:):Int {5};
  doh() : Int { (let i : Int <- h, j : Int, m: Int <- 4 in { h <- h + 2; i; let x in x+1; } ) };
  doh() : Int { (let i : In4t <- h) };
};

class Test extends Object {
  foo:Test;
};

class Test {
  foo:Test;
  bar():Int {5};
};

class Test implements Nothing{
  foo:Test;
  bar():Int {5};
};


